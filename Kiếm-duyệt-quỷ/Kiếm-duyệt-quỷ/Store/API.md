**API**: là tập hợp các điểm endpoint: 
		- endpoint? là điểm tiếp nhận của req của server, là điểm mà client và server giao tiếp với nhau
		- endpoint = địa chỉ + cách gọi để client giao tiếp với backend
		-                 = METHOD + URL (GET https://api.myapp.com/users)